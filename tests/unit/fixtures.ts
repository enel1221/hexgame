import type { MatchConfig } from "../../src/shared/types";

export const TEST_CONFIG: MatchConfig = {
  seed: "unit-test-seed",
  archetype: "heartland",
  aiCount: 3,
  difficulty: "normal",
  playerName: "Tester",
  graphics: "low",
  sound: false,
  colorPatterns: true,
  debug: false,
};
